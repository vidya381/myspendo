# Database Configuration
#
# OPTION 1: Use DATABASE_URL
# Simply copy the connection string from your database provider
# Example: postgresql://user:password@ep-cool-name.us-east-2.aws.neon.tech/myspendo?sslmode=require
# DATABASE_URL=your_connection_string_here
#
# OPTION 2 (For local development): Use individual variables
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=myspendo
DB_SSLMODE=disable

# JWT Secret (MUST be a strong random secret in production)
# Generate with: openssl rand -hex 32
JWT_SECRET=generate_a_strong_random_secret_here

# CORS Origin (Frontend URL)
# For local development: http://localhost:3000
# For production: https://your-app.vercel.app
CORS_ORIGIN=http://localhost:3000
